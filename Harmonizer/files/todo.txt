crear una clase intervalo
crear la clase escala, la cual es una lista de intervalos
dada una lista de notas y una tonica, 
    intercambiar cada nota por su correspondiente intervalo (averiguar el grado de cada nota)
dada una escala, averiguar todas sus triadas posibles (y cuatriadas)
dadas una serie de notas y una tónica, averiguar el esqueleto de la escala (si es una escala mayor o menor, por ahoras)
dada una lista de notas normalizadas (transformadas en grados), dividir la canción en intervalos de tiempo uniformes
    y averiguar que acorde le corresponde a cada intervalo de tiempo

el readMidi funciona mal con notas de 0 de duración
resolver el siguiente problema: dado un conjunto de menos de 7 notas, 
    ¿qué escalas de 7 notas contienen ese conjunto?
    ¿qué escalas de 7 notas relativas a una escala mayor contienen ese conjunto?

mirar el pitch medio e imprimir los acordes en una octava inferior, hay que tenr en cuenta el pitch de la tónica para separar
    las octavas de tónica a tónica y no de do a do
si hay varias escalas coincidentes valorar cual es la correcta dependiendo de la frcuencia de la tónica